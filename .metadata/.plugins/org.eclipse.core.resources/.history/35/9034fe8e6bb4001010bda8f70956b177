/*
 * File: Util.h
 * Driver Name: [[ Util ]]
 * SW Layer:   Util
 * Created on: Jun 28, 2020
 * Author:     Khaled Magdy
 * -------------------------------------------
 * For More Information, Tutorials, etc.
 * Visit Website: www.DeepBlueMbedded.com
 */

#ifndef UTIL_H_
#define UTIL_H_

#include "stdint.h"
/*
*---------------------------------------
*   GPIO Direct Register Access Macros
*---------------------------------------
*/

#define GPIO_SET_PIN(port, pin) 	((port)->BSRR = (pin))
#define GPIO_CLEAR_PIN(port, pin) 	((port)->BSRR = (pin << 16u))
#define GPIO_TOGGLE_PIN(port, pin) 	((port)->ODR  ^= (pin))
#define GPIO_READ_PIN(port, pin) 	((port)->IDR & (pin))

/*
*---------------------------------------
*   SysTick Timer Delay Macros
*---------------------------------------
*/

#define SYSTICK_LOAD (SystemCoreClock/1000000U)
#define SYSTICK_DELAY_CALIB (SYSTICK_LOAD >> 1)

void UTIL_Init(void);           // llamar una vez al inicio (main)
void UTIL_DelayUs(uint32_t us);
#define DELAY_US(us) UTIL_DelayUs(us)
#define DELAY_MS(ms) HAL_Delay(ms)

static void utoa_dec(int32_t val, char *buf);

#endif /* UTIL_H_ */
